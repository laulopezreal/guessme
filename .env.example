# LLM API Configuration (required for AI mode)
VITE_LLM_API_KEY=your_api_key_here
VITE_LLM_MODEL=gpt-4o-mini

# Game Mode Configuration
# Set to 'classic' for static clue-based gameplay
# Set to 'ai' for LLM-powered conversational gameplay
# Defaults to 'classic' if not set or if LLM is not configured
VITE_GAME_MODE=ai
